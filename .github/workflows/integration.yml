name: Integration & Migration Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  migration-verification:
    name: Database Migrations & Seed Verification
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: test_secure_password
          POSTGRES_DB: vertexerp_integration
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run Alembic Upgrade to Head
        env:
          DATABASE_HOST: localhost
          DATABASE_PORT: 5432
          DATABASE_USER: postgres
          DATABASE_PASSWORD: test_secure_password
          DATABASE_NAME: vertexerp_integration
        run: |
          alembic upgrade head

      - name: Run Schema Downgrade and Re-Upgrade Test
        env:
          DATABASE_HOST: localhost
          DATABASE_PORT: 5432
          DATABASE_USER: postgres
          DATABASE_PASSWORD: test_secure_password
          DATABASE_NAME: vertexerp_integration
        run: |
          alembic downgrade -1
          alembic upgrade head
